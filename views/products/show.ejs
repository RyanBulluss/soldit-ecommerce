<%- include('../partials/header') %>
<%- include('../partials/title') %>

<div class="container card p-5 mx-auto">
    <div class="row">
        <div class="col-md-5 d-flex flex-column justify-content-center align-items-center">
            <image class="show-image border" src="https://img.freepik.com/premium-photo/white-tshirt-white-background-front-back-view-generative-ai_446633-7047.jpg?w=740"></image><br>
        </div>
        <div class="col-md-3 d-flex flex-column  justify-content-between">
                <h4 class="mb-0"><%= product.name %></h3>
                <% const ratingsAverage = product.reviews.reduce((acc, review) => acc + review.rating, 0) / product.reviews.length %>
                Rating: <%= ratingsAverage ? ratingsAverage : 'No ratings' %> <br>
                <h5>£<%= product.price %></h5>
                <br><br>
            <span>Quality: <%= product.quality %></span> <br>
            <span>Category: <%= product.category %></span> <br>
            
        </div>
        <div class="col-md-3  d-flex flex-column justify-content-center align-items-center">
            <span>Seller: <%= product.userName %></span> <br>
            <span>Stock: <%= product.quantity %></span> <br> 
            <form action="<%= product._id %>/add" method="POST">
              <label>Quantity:</label>
              <input name="quantity" value="1" min="1" max="<%= product.quantity %>" type="number"> <br>
              <button type="submit" class="btn mt-1 btn-warning">Add To Basket</button>
            </form>
            <br>
          </div>
          <div class="col-md-1"></div>
        
    </div>
    <div class="row">
        <div class="col-md-8 mx-auto">
            description
        </div>

    </div>

    <div class="row">
        
    </div>
    <% if(user?.name == product.userName) { %>
        <form action="<%= product._id %>/delete?_method=DELETE" method="POST">
            <button type="submit" class="mx-auto btn mt-3 btn-danger">Delete Product</button>
        </form>
    <% } %>

    <% if(product.reviews.length) { %>
    <h4 class="mt-4 ">Reviews:</h4>
    <table class="table">
        <tbody>
          <% product.reviews.forEach(function(review) { %>
          <tr>
            <td><img src="<%= review.userAvatar %>" alt="User Avatar" class="rounded-circle" style="width: 50px; height: 50px;" referrerpolicy="no-referrer"></td>
            <td><%= review.userName %></td>
            <td><%= review.rating %></td>
            <td><%= review.review %></td>
            <td>00/00/00</td>
            <td>
              <% if(user?._id.equals(review.user)) { %>
              <form action="<%= review._id %>?_method=DELETE" method="POST">
                <button type="submit" class="btn btn-danger">X</button>
              </form>
              <% } %>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
        <h5>No Reviews Yet</h5>
    <% } %>
    <% if (user) { %>
        <a class="text-center" href="<%- product.id %>-new-review">New Review</a>
    <% } %>
</div>

<%- include('../partials/footer') %>